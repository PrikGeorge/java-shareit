
  #logging.level.org.apache.http=DEBUG
  #logging.level.httpclient.wire=DEBUG
logging:
  level:
    org:
      springframework:
        web:
          client:
            RestTemplate: DEBUG

server:
    port: 8080

shareit-server:
    url: http://localhost:9090
